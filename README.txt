dependency reqs:

mongoose
nodemon (localhost server)
dotenv
express

"npm run a" in git bash terminal to start it, then go to localhost:5000

going to localhost:5000/tasks returns the json of the entire task object